# CLAUDE.md 本文件为 Claude Code (claude.ai/code) 提供操作指引

## 项目状态（下个会话从这里对齐）

- **当前进度**：需求设计已完成（`/grill-with-docs` 13 问对齐）。**下个会话的目标：按 `docs/PRD.md` 动手实现 skill。**
- **权威需求源**：`docs/PRD.md`（产品需求）、`CONTEXT.md`（领域术语表）、`docs/adr/0001`（可选依赖决策）。
- **skill 落点**：`.agents/skills/user-manual/`，命名 `user-manual`，带 `agents/openai.yaml`。



## Git Diff 优先（关键）
**当用户在对话中提到“改了代码”、“做了修改”、“更新了”，或 git status 显示有改动时，AI 必须遵循以下优先级：**

1. **先看 diff**：`git diff` 或 `git diff --cached` 查看所有改动内容。这是最准确的信息来源。
2. **diff 不够再看文件**：只有在 diff 片段无法判断上下文时（例如改动影响了其他模块、需要了解调用链），才去读取相关文件。
3. **禁止跳过 diff 直接探索**：不允许一上来就用 Glob/Grep/Read 到处找“可能相关的代码”——diff 已经精确告诉你改了哪里。

**原因**：diff 是用户意图的直接体现。跳过 diff = 忽略用户已经做的事情，浪费 token 和时间去猜测。先看 diff，AI 的回答才能精准命中用户的实际改动。

漏了检查被用户指出 → 停止辩解，修复根因，添加新约束到 memory。

## AI 辅助开发工具

本项目已由多个代码智能系统建立索引。在手动搜索或读取文件**之前**，请优先使用这些工具——它们比 grep/Read 循环更快、更准确。

### CodeGraph（首要代码浏览器）（特别重要！！！）

`.codegraph/` 索引覆盖了仓库中的每个符号、每条边和每个文件。
对于几乎所有问题——“X 是如何工作的”、架构问题、Bug 排查、或在编辑代码之前——
都可以调用 `codegraph_explore`（MCP）或 `codegraph explore`（shell）。
一次调用通常就能返回所有相关符号的逐字源码，以及它们之间的调用路径，
包括 grep 无法追踪的动态分发跳转。将返回的源码视为已读取——无需再次打开这些文件。

**优先使用 CodeGraph。** 它可以替代大多数 grep/Read 循环。

### Fetch MCP（网络调研）

`mcp__fetch__fetch` 工具可以抓取网页并将其提取为 markdown 格式。
适用于：
- 查阅 Android / Compose / Kotlin API 文档
- 查看库的更新日志或迁移指南
- 从 Stack Overflow、GitHub Issues 等来源调研解决方案

当你确切知道 URL 时，优先使用 Fetch 而非 WebSearch。
需要先发现 URL 时，使用 WebSearch。


## 问题记录触发准则
凡遇以下任一场景，必须启动问题记录流程：
1. **执行异常**：任务中断、Agent 行为偏差（如死循环、幻觉）或产出结果非预期。
2. **审查缺陷**：在 Code Review、人工测试或自动化检查中发现的逻辑漏洞、安全风险或规范违规。
3. **运行阻塞**：实际使用或集成过程中遇到的环境报错、配置冲突或 API 调用失败。
**触发条件**：任何涉及执行失败、审查发现问题或运行时阻碍的异常事件，均需进行归档记录。
**记录模版**："docs\problems-records.md"

## agent 要求（特别重要！！！）
- 不要急着生成代码，改文件。你应该优先引导用户，有明确指令才能动手。就算是plan模式，也要在计划生成完之后停下，等用户先审阅。
- 生成代码是要带上注释。要求：中文；要工程化；要精简、清晰且准确明了。
- 你有问题的话你得问我，遇到不确定的事情，不要直接自己决定，问我
- 不要主动 git commit
- Agent 可以根据项目发展状况与总体情况做出判断，并主动提出修改 Agent 文件（如 `CLAUDE.md`、`AGENTS.md`）的建议；但在实际修改前，必须询问用户并获得明确同意。

## 代码结构原则：

1. **单一文件职责**：每个文件只承载一个明确功能模块（组件、工具、页面逻辑等），可独立拆分的逻辑必须分文件存放，通过模块导入组合。

2. **层级目录组织**：按业务从属关系使用文件夹嵌套，主页面与子页面、详情页等归属明确的页面置于同一父级目录下，形成树状层级（如 `pages/主/子/`），避免扁平堆积。

3. **依赖方向单向**：上层模块可依赖下层，下层不得反向依赖上层；同一层级间保持平行，禁止跨层级直接引用内部实现。

4. **命名与边界**：目录名语义化（如 `components`、`hooks`、`sub-pages`），路径即定位，确保依赖边界清晰可控。

## 前端设计
- 我需要界面能够自适应屏幕，所以你得自动把具体的数据转化成对应的比例，然后按比例分布。
  - **必须按比例换算**：所有占画布比例的几何量——元素位置（x/y）、宽度、横向留白/间距、卡片宽度等
  - **可硬编码固定值**：字体大小、行间距、图标尺寸等固定视觉量
  - **禁止直接写死设计稿 dp 数值**（如 `padding = 77.dp`、`width = 273.dp`），小屏上会导致换行、越界或重叠（教训见 `docs/problems/welcome-page-overlap.md`）
- 如果图标找不到的话，那就告诉我，不要自己用 Canva 绘制
- 进行前端设计时，所有的颜色都用主题色，不要硬编码颜色。如果主题色里面没有颜色，你可以去主题色里面加颜色，但是要写好注释

## 子 agent 调用
- 在进行诸如写入代码这类简单，开销大的任务时，调用便宜的模型用子agent完成（如：deepseek-v4-flash），节省成本同时保持主对话窗口的纯净与完整
- 在合理范围内更多地主动应用子agent去完成任务，而不是一个窗口干所有活



## 动态适应（关键）
AI 必须持续追踪和调整以下维度：
- **已完成**：用户已实现并验证的功能模块
- **进行中**：当前正在开发的任务
- **卡点**：遇到困难、需要返工或额外学习的地方
- **下一步**：基于实际进度动态调整优先级，而非死守 PRD 顺序
- **开发速度**：根据用户的产出节奏调整排期预估

PRD.md 是起点蓝图，不是固定合同。每次 session 结束后更新 memory 文件。

